name: Deploy
on:
  workflow_dispatch:
  push:
    branches:
      - master
jobs:
  build:
    name: Run API
    runs-on: ubuntu-latest
    steps:
      - name: Build API
      - uses: actions/checkout@v3
        run: |
          echo "Run script"
          sh ./common.sh
